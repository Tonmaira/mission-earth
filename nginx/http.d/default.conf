# upstream backend_servers {
#     # Define the backend server(s)
#     # Adjust the port to match your Node.js application's port
#     #server backend:3000; # Test Server
#     #server backend:3500 backup; # Build Server
#     server app:3500; # Assuming `app` is the name of your Node.js service
# }

# server {
#     listen 80;
#     server_name localhost; # Use localhost or a specific domain name
#     client_max_body_size 100M;
#     # Handle WebSocket connections for /broadcast
#     location /socket.io/ {
#         proxy_pass http://app:5000/socket.io/;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }

#     # Proxy /api/ requests to the backend API
#     location /api/ {
#         proxy_pass http://app:5000; # Adjust if your API is on a different port
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }

#     # Default handling for all other requests
#     # location / {
#     #     proxy_pass http://backend_servers; # Use the upstream defined above
#     #     proxy_set_header Host $host;
#     #     proxy_set_header X-Real-IP $remote_addr;
#     #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     #     proxy_set_header X-Forwarded-Proto $scheme;
#     # }

#     # Frontend
#     location / {
#         root /app/frontend/dist;
#         index index.html;
#         try_files $uri $uri/ /index.html;
#     }

#     # Define error pages
#     error_page 500 502 503 504 /50x.html;
#     location = /50x.html {
#         root /usr/share/nginx/html;
#     }
# }

upstream nextjs {
    server app:3000;   # Next.js container
}

upstream backend_api {
    server app:5000;   # Node backend container (หรือ service แยก)
}

server {
    listen 80;
    server_name localhost;
    client_max_body_size 100M;

    # WebSocket (socket.io)
    location /socket.io/ {
        proxy_pass http://backend_api/socket.io/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # API route
    location /api/ {
        proxy_pass http://backend_api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Next.js frontend (SSR / static)
    location / {
        proxy_pass http://nextjs;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}